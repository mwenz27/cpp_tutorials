#include <cstdio>
using namespace std;

int main()
{
    int ia[] = { 1, 2, 3, 4, 5 };
    int x = 7;
    int y = 42;
    int *ip = &y;
    int &z = x;
    char s[]  = "string";
//
//    for( auto i : ia ) {
//        printf("i is %d\n", i);
//        printf("i is %d\n", i*i);
//    }
    printf("y is is %d\n", y);
    printf("x is is %d\n", x);
    printf("*ip is is %d\n", *ip);
    printf("z is is %d\n",z);
    printf("string is %s\n", s);
    printf("*ip is is %d\n", *ip);

    if (x > y) {
        puts("condition is true");
    } else if (y > 30){
        puts("y is greater 30");
    }

    printf("the greater value is %d\n", x > y ? x : y);
    return 0;
}
